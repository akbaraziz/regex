For User Name:
Please enter a user name to login to the system. User name must not contain spaces or special characters like $#%@&;*(){}+
regex=\w{5,255}

For User Password:
Please enter a password for the new user created. Password must be at least 8 characters, no more than 15 characters, and must include at least one upper case letter, one lower case letter, and one numeric digit.
regex=^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,15}$